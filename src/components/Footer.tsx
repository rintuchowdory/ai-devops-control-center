export default function Footer() {
  return (
    <footer className="border-t border-line/60">
      <div className="mx-auto max-w-6xl px-6 py-10 flex flex-col md:flex-row items-start md:items-center justify-between gap-4">
        <p className="font-mono text-xs text-fog">
          Phase 1 — landing scene, harbor, islands, monitoring city, assistant UI. Live cluster
          connection lands in Phase 3.
        </p>
        <p className="font-display text-sm text-mist">AI DevOps Control Center</p>
      </div>
    </footer>
  );
}
